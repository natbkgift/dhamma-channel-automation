# 🧹 TTS Text Preprocessor Agent

## ภาพรวม

**TTS Preprocessor Agent** เป็นระบบทำความสะอาดและปรับแต่งข้อความอัตโนมัติก่อนส่งไปสร้างเสียง เพื่อให้เสียง AI ออกมา:
- **ธรรมชาติ**: ไม่มีเสียงแปลก อ่านเครื่องหมายพิเศษ
- **ชัดเจน**: ออกเสียงตัวเลขและคำย่อได้ถูกต้อง
- **เหมาะสม**: เหมาะกับช่องธรรมะ ไม่อ่านแทกหรืออีโมจิ

---

## ⚡ Quick Start

### วิธีที่ 1: ใช้ผ่าน Production Guide HTML (แนะนำ!)

1. เปิด `output/production_complete_001/PRODUCTION_GUIDE.html`
2. เลื่อนไปที่ **Step 1: Voiceover Recording**
3. ในส่วน **🤖 สร้างเสียงบรรยายด้วย AI**:
   - ✅ เลือก **"🧹 ทำความสะอาดข้อความ"** (checked by default)
   - เลือก Provider: Google Cloud TTS
   - เลือก Voice: th-TH-Neural2-C
4. คลิก **"สร้างเสียงด้วย AI"**

### วิธีที่ 2: ใช้ผ่าน Command Line

```bash
# แบบเต็ม: ทำความสะอาด + สร้างเสียง
python scripts/tts_unified.py \
  --provider google \
  --script "audio/production_complete_001/recording_script_SIMPLE.txt" \
  --output "audio/voiceover_ai.mp3" \
  --voice th-TH-Neural2-C \
  --clean

# Preview เฉพาะข้อความที่ทำความสะอาดแล้ว (ไม่สร้างเสียง)
python scripts/tts_unified.py \
  --provider google \
  --script "audio/production_complete_001/recording_script_SIMPLE.txt" \
  --clean \
  --preview

# ใช้ Preprocessor แยก
python scripts/tts_preprocessor.py \
  --input "audio/production_complete_001/recording_script_SIMPLE.txt" \
  --output "audio/cleaned_script.txt"
```

---

## 🔧 สิ่งที่ Agent ทำ

### 1️⃣ ลบเนื้อหาที่ไม่ควรอ่านออกเสียง

#### ❌ Markdown & HTML
```markdown
# หัวข้อ          → หัวข้อ
**ตัวหนา**        → ตัวหนา
*ตัวเอียง*        → ตัวเอียง
[ลิงก์](url)      → ลิงก์
<div>text</div>   → text
```

#### ❌ แทกและ Mentions
```
#ธรรมะ #สติ      → (ลบทั้งหมด)
@username        → (ลบทั้งหมด)
```

#### ❌ อีโมจิและสัญลักษณ์พิเศษ
```
🎯 เป้าหมาย      → เป้าหมาย
✅ สำเร็จ         → สำเร็จ
★☆✓✔            → (ลบทั้งหมด)
• รายการ         → รายการ
1. รายการ        → รายการ
```

#### ❌ URLs
```
https://example.com    → (ลบทั้งหมด)
www.example.com       → (ลบทั้งหมด)
```

### 2️⃣ ขยายคำย่อ

#### ภาษาไทย
```
ม. → มหาวิทยาลัย
ดร. → ดอกเตอร์
พ.ศ. → พุทธศักราช
ค.ศ. → คริสต์ศักราช
จ. → จังหวัด
อ. → อำเภอ
```

#### ภาษาอังกฤษ
```
Mr. → Mister
Mrs. → Missis
Dr. → Doctor
Prof. → Professor
```

### 3️⃣ แปลงตัวเลขเป็นคำไทย

```
1 → หนึ่ง
5 → ห้า
10 → สิบ
15 → สิบห้า
20 → ยี่สิบ
21 → ยี่สิบเอ็ด
100% → หนึ่งร้อย เปอร์เซ็นต์
```

### 4️⃣ ปรับเครื่องหมายวรรคตอน

```
ไม่มีspace,ต่อเลย      → ไม่มีspace, ต่อเลย
ข้อความ!!!              → ข้อความ!
อะไรนะ???               → อะไรนะ?
ข้อความ...              → ข้อความ.
ข้อความ:ต่อเลย          → ข้อความ. ต่อเลย
```

### 5️⃣ แบ่งประโยคยาว

- ประโยค >150 ตัวอักษร → แบ่งด้วย comma
- ช่วยให้ TTS chunking ทำงานได้ดีขึ้น
- เสียงฟังธรรมชาติกว่า (มีจังหวะหายใจ)

---

## 📊 ผลการทดสอบ

### Input: สคริปต์ธรรมะต้นฉบับ
```
ขนาด: 9,759 ตัวอักษร (17,903 bytes)
มี: emoji, markdown, แทก, ประโยคยาว
```

### Output: หลังทำความสะอาด
```
ขนาด: 10,029 ตัวอักษร (19,532 bytes)
เพิ่มขึ้น: 2.8% (จากการขยายคำย่อและแปลงตัวเลข)
ลบ: emoji, แทก, markdown ทั้งหมด
ขยาย: 3 คำย่อ (พ.ศ., ม., ดร.)
แปลง: ตัวเลข 1-100 เป็นคำไทย
แบ่ง: 18 ประโยคยาว
```

### ผลลัพธ์เสียง
```
ไฟล์: audio/test_google_cleaned.mp3
ขนาด: 9.35 MB
chunks: 5 ส่วน
ค่าใช้จ่าย: $0.1605 (~5.62 บาท)
คุณภาพ: ⭐⭐⭐⭐⭐ เสียงธรรมชาติ ไม่มีเสียงแปลก
```

---

## 🎯 Use Cases

### 1. วิดีโอธรรมะ (ใช้เสมอ!)
```bash
python scripts/tts_unified.py \
  --provider google \
  --script "script.txt" \
  --output "voice.mp3" \
  --voice th-TH-Neural2-C \
  --clean  # ← ต้องมี!
```

**ทำไมต้องใช้**:
- ลบแทก (#ธรรมะ #สติ)
- ลบอีโมจิ (🙏 ✨)
- แปลงตัวเลข (5 นาที → ห้านาที)
- เสียงออกมาเหมือนพระอาจารย์อ่านจริงๆ

### 2. สคริปต์จาก Social Media
```bash
python scripts/tts_unified.py \
  --script "facebook_post.txt" \
  --clean \
  --preview  # ดูก่อนว่าทำความสะอาดได้ดีไหม
```

**จัดการ**:
- ลบ @mention
- ลบ hashtags
- ลบ emoji ทั้งหมด
- ลบลิงก์

### 3. บทความ/Blog
```bash
python scripts/tts_preprocessor.py \
  --input "article.md" \
  --output "article_cleaned.txt"
```

**จัดการ**:
- ลบ markdown formatting
- ขยายคำย่อ
- แบ่งประโยคยาว

---

## 🔍 Advanced Features

### Custom Dhamma Terms (อนาคต)

Agent มี dictionary สำหรับคำธรรมะพิเศษ:

```python
dhamma_terms = {
    'พระพุทธเจ้า': 'พระ-พุทธ-เจ้า',
    'อนาปานสติ': 'อะนา-ปา-นะ-สะติ',
    'วิปัสสนา': 'วิ-ปัด-สะนา',
    # เพิ่มได้ตามต้องการ
}
```

**วิธีเพิ่ม**:
1. เปิด `scripts/tts_preprocessor.py`
2. แก้ไข `self.dhamma_terms` ใน `__init__`
3. บันทึกไฟล์

### Analyze Text

```bash
python scripts/tts_preprocessor.py \
  --input "script.txt" \
  --analyze  # (ยังไม่มี - ต้องเพิ่ม)
```

ดูสถิติ:
- จำนวนแทก
- จำนวนอีโมจิ
- จำนวน URLs
- ประโยคยาวที่สุด

---

## ⚙️ Configuration

### ปรับความยาวประโยคสูงสุด

แก้ใน `scripts/tts_preprocessor.py`:

```python
# เดิม
text = self._split_long_sentences(text, max_length=150)

# เปลี่ยนเป็น
text = self._split_long_sentences(text, max_length=200)  # ยาวขึ้น
```

### ปิดการแปลงตัวเลข

Comment บรรทัดนี้:

```python
# text = self._process_numbers(text)
```

### เพิ่มคำย่อใหม่

แก้ `self.abbreviations`:

```python
self.abbreviations = {
    # เดิม...
    
    # เพิ่มใหม่
    'ผอ.': 'ผู้อำนวยการ',
    'รพ.': 'โรงพยาบาล',
}
```

---

## 📁 ไฟล์ที่เกี่ยวข้อง

```
scripts/
├── tts_preprocessor.py          # Agent หลัก
├── tts_unified.py                # Unified TTS (รองรับ --clean)
├── tts_generator.py              # OpenAI TTS
└── tts_generator_google.py       # Google TTS

output/production_complete_001/
├── PRODUCTION_GUIDE.html         # UI พร้อม checkbox
└── recording_script_SIMPLE.txt   # สคริปต์ต้นฉบับ

audio/production_complete_001/
├── recording_script_SIMPLE.txt         # ต้นฉบับ
├── recording_script_SIMPLE_cleaned.txt # หลังทำความสะอาด (auto)
└── recording_script_TTS_READY.txt      # ทำความสะอาดแยก (manual)
```

---

## 🎓 Best Practices

### ✅ ควรใช้ --clean เมื่อ:

1. **สคริปต์มาจาก Social Media**
   - มีแทก, emoji, mention
   
2. **สคริปต์มี Markdown/HTML**
   - มาจาก blog, documentation
   
3. **สคริปต์มีตัวเลข**
   - "5 นาที" ดีกว่า "five minutes" (ภาษาไทย)
   
4. **สคริปต์ธรรมะ** (ใช้เสมอ!)
   - เสียงธรรมชาติที่สุด

### ❌ ไม่ต้องใช้ --clean เมื่อ:

1. **สคริปต์สะอาดแล้ว**
   - เขียนเป็น plain text ตั้งแต่ต้น
   
2. **ต้องการควบคุมเอง**
   - มีรูปแบบพิเศษที่ต้องการเก็บไว้

### 🔍 ใช้ --preview ก่อนเสมอ (ครั้งแรก)

```bash
# ดูผลก่อนสร้างเสียง
python scripts/tts_unified.py \
  --script "script.txt" \
  --clean \
  --preview
```

ตรวจสอบ:
- ข้อความถูกลบมากเกินไปไหม?
- ตัวเลขแปลงถูกต้องไหม?
- ประโยคถูกแบ่งดีไหม?

---

## 🐛 Troubleshooting

### ❌ ข้อความหายไปมาก

**สาเหตุ**: Agent ลบเนื้อหาที่สำคัญ

**แก้ไข**: 
1. ใช้ `--preview` ดูก่อน
2. ปรับ `removal_patterns` ใน `tts_preprocessor.py`
3. หรือไม่ใช้ `--clean`

### ❌ ตัวเลขแปลงผิด

**สาเหตุ**: รองรับเฉพาะ 1-100

**แก้ไข**:
1. แปลงเองก่อน (1000 → หนึ่งพัน)
2. หรือ comment `_process_numbers()` ออก

### ❌ คำย่อไม่ขยาย

**สาเหตุ**: ยังไม่มีใน dictionary

**แก้ไข**: เพิ่มใน `self.abbreviations`

---

## 📊 Comparison: ก่อนและหลัง

### ก่อนใช้ Preprocessor ❌

```
สคริปต์:
"🙏 สวัสดีครับ! วันนี้เรามาเรียนรู้เรื่อง #สติ #mindfulness กันครับ
มี 5 ขั้นตอนดังนี้:
1. หายใจเข้า
2. หายใจออก
ดูรายละเอียดที่ https://example.com"

เสียงที่ได้:
"สวัสดีครับ แฮชแท็ก-สติ แฮชแท็ก-ไมน์-ฟูล-เนส มีห้าขั้นตอน..."
→ เสียงแปลก อ่านแทกและลิงก์ 😵
```

### หลังใช้ Preprocessor ✅

```
สคริปต์ (หลัง clean):
"สวัสดีครับ! วันนี้เรามาเรียนรู้เรื่องสติ กันครับ
มีห้าขั้นตอนดังนี้
หายใจเข้า. หายใจออก."

เสียงที่ได้:
"สวัสดีครับ! วันนี้เรามาเรียนรู้เรื่องสติกันครับ มีห้าขั้นตอนดังนี้..."
→ เสียงธรรมชาติ ฟังสบาย 🎯
```

---

## 🚀 Next Steps

### Phase 1: ใช้งานพื้นฐาน ✅
- [x] สร้าง Agent พื้นฐาน
- [x] เชื่อมกับ tts_unified.py
- [x] เพิ่ม checkbox ใน HTML
- [x] ทดสอบกับสคริปต์จริง

### Phase 2: Advanced Features (Optional)
- [ ] เพิ่ม SSML support (ปรับ tone, emphasis)
- [ ] ใช้ AI ตรวจจับบริบท (เก็บแทกบางตัวไว้)
- [ ] รองรับหลายภาษาดีขึ้น
- [ ] Custom rules per channel type

### Phase 3: Integration (Future)
- [ ] เพิ่มใน Production Pipeline
- [ ] Auto-clean ทุกสคริปต์ก่อนสร้างเสียง
- [ ] Batch processing

---

## ✅ สรุป

**TTS Preprocessor Agent** ช่วยให้:
- ✅ เสียง AI ออกมาธรรมชาติ
- ✅ ไม่อ่านแทก, อีโมจิ, ลิงก์
- ✅ แปลงตัวเลขและคำย่อถูกต้อง
- ✅ เหมาะกับช่องธรรมะ
- ✅ ใช้งานง่าย (checkbox เดียว!)

**คำแนะนำ**: เปิด checkbox "🧹 ทำความสะอาดข้อความ" เสมอเมื่อสร้างเสียงสำหรับ production!
