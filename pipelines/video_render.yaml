pipeline: video_render
version: 0.1
steps:
  - id: voiceover_tts
    uses: voiceover.tts
    config:
      slug: voiceover_demo
      script_path: scripts/voiceover_script.txt
      dry_run: false

  - id: video_render
    uses: video.render
    config:
      slug: voiceover_demo
      dry_run: false
      resolution: "1920x1080"
      fps: 30
      bg_color: "black"
