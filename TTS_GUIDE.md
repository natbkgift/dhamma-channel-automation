# คู่มือการใช้งาน AI Text-to-Speech (TTS)

## 🎙️ วิธีสร้างเสียงบรรยายด้วย OpenAI TTS

### 1. ตั้งค่า API Key

แก้ไขไฟล์ `production_config.json`:

```json
{
  "openai_api_key": "sk-xxxxxxxxxxxxxxxxxxxxx"
}
```

**รับ API Key ฟรี:**
1. ไปที่ https://platform.openai.com/api-keys
2. สร้างบัญชี (มี $5 free credit)
3. คลิก "Create new secret key"
4. คัดลอก key และวางใน `production_config.json`

**ราคา:** ~$15 ต่อ 1 ล้านตัวอักษร (ประมาณ 0.30 บาทต่อวิดีโอ)

### 2. วิธีใช้งาน

#### แบบที่ 1: กดปุ่มใน Production Guide (แนะนำ)

1. เปิดไฟล์ `PRODUCTION_GUIDE.html`
2. ไปที่ Step 1: Voiceover Recording
3. กดปุ่ม **"🎙️ สร้างเสียงด้วย AI"**
4. คัดลอกคำสั่งที่แสดง
5. รันใน Terminal/PowerShell

#### แบบที่ 2: รันคำสั่งโดยตรง

```powershell
cd "D:\Auto Tool\dhamma-channel-automation"
venv\Scripts\activate
python scripts/tts_generator.py ^
  --script "audio/production_complete_001/recording_script_SIMPLE.txt" ^
  --output "audio/production_complete_001/voiceover_ai.mp3" ^
  --voice alloy ^
  --speed 1.0
```

### 3. เสียงที่เลือกได้ (Voices)

- **alloy** - เสียงกลางๆ เป็นกันเอง (แนะนำ)
- **echo** - เสียงชัดเจน มั่นใจ
- **fable** - เสียงอบอุ่น น่าฟัง
- **onyx** - เสียงทุ้ม มีพลัง
- **nova** - เสียงสดใส กระฉับกระเฉง
- **shimmer** - เสียงนุ่มนวล ละมุน

เปลี่ยนเสียงด้วย `--voice <ชื่อเสียง>`

### 4. ปรับความเร็ว

- **0.25** - ช้ามาก (สำหรับการเรียนรู้)
- **0.75** - ช้าพอดี
- **1.0** - ปกติ (แนะนำ)
- **1.25** - เร็วขึ้นเล็กน้อย
- **2.0** - เร็วมาก
- **4.0** - เร็วสุด

เปลี่ยนความเร็วด้วย `--speed <ตัวเลข>`

### 5. ตัวอย่างคำสั่ง

**เสียงอบอุ่น ช้าๆ:**
```powershell
python scripts/tts_generator.py --script "audio/.../recording_script_SIMPLE.txt" --output "audio/.../voiceover.mp3" --voice fable --speed 0.9
```

**เสียงชัดเจน เร็วขึ้น:**
```powershell
python scripts/tts_generator.py --script "audio/.../recording_script_SIMPLE.txt" --output "audio/.../voiceover.mp3" --voice echo --speed 1.2
```

### 6. หมายเหตุ

- ✅ รองรับภาษาไทย (OpenAI TTS รองรับ 57 ภาษา)
- ✅ คุณภาพสูง (24kHz sampling rate)
- ⚠️ ข้อความต้องไม่เกิน 4096 ตัวอักษร (ถ้ายาวเกินจะแบ่งเป็นหลายส่วน)
- ⚠️ ต้องเชื่อมต่ออินเทอร์เน็ต
- ⚠️ ใช้ credit ใน OpenAI account

### 7. ทางเลือกอื่น (ฟรี)

ถ้าไม่ต้องการใช้ OpenAI TTS สามารถใช้:

1. **Google Cloud TTS** - มี free tier 1 ล้านตัวอักษร/เดือน
2. **Coqui TTS** - Local, ฟรี, แต่ต้องติดตั้งโมเดล
3. **Piper TTS** - Local, เร็ว, คุณภาพดี
4. **บันทึกเองด้วย Audacity** - คุณภาพสูงสุด แต่ใช้เวลา

---

💡 **Tips:** ลอง generate เสียงหลายแบบ แล้วเลือกที่ชอบที่สุด!
